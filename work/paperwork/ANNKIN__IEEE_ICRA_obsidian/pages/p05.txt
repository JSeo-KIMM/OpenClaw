                                                                           Fig. 7. Experimental setup for evaluating motion tracking accuracy in a
Fig. 6. Validation errors vs. number of training samples. Rapid decrease   teleoperation scenario, where arbitrary motions of the master device and the
followed by saturation beyond 10,000 samples (red dashed line) for all     corresponding motions of the slave robot were tracked by the OTS
translational and rotational axes


encoder angles (θ). The encoder angle is fed into the ANN
model in its normalized form (θ̂), and the model predicts the
corresponding normalized translation and rotation (ŷ = [t̂, r̂])
as follows:

                       ŷ = f (θ̂; M ) = [t̂, r̂]                   (4)

  Then, (ŷ) is de-normalized using the model’s range infor-
mation, resulting in (y = [t, r]) as,

               t = t̂ · (max(t) − min(t)) + min(t)
                                                                    (5)    Fig. 8. Schematic diagram of the experimental setup for tracking master
              r = r̂ · (max(r) − min(r)) + min(r)                          device manipulation and corresponding robot arm motion. This schematic
                                                                           outlines the system: master encoder angles are processed by an ANN to drive
   Here, (y = [t, r]) represents the pose of the top plate center          the robot arm’s TCP, while an OTS simultaneously tracks both for accuracy
of the master device. This data is then transmitted to the robot           evaluation
arm controller, where it serves as the tool center point (TCP)
of the serial robot arm. Specifically, the robot arm controller
stores the initial TCP (tinit , rinit ) as the reference when the
system starts up. Each time a new pose (t, r) is received, the             Data Exchange (RTDE) interface was employed for its real-
relative transformation is computed as,                                    time position control. Figure 8 illustrates the connection and
                                                                           interaction between the components of the system. Specifi-
                            ∆t = t − tinit ,
                                                                    (6)    cally, when the master device was manipulated by the user,
                           ∆r = r − rinit .                                the encoder angles received from the master device controller
The transformation represents the required motion adjustment,              were transmitted to the kinematic solver in the robot control
which is then sent as a control command for execution to up-               unit. The kinematic solver processed the six encoder angles
date a new TCP. In the developed system, the aforementioned                using the trained ANN model to compute the pose of the
processes are executed at a speed of 1 kHz. A detailed analysis            master device’s top plate center. The calculated pose was then
of processing time is provided in Section IV.C.                            sent to the robot arm controller to move the TCP of the
                                                                           robot arm accordingly. The OTS (Atracsys FTK500, Atracsys
                 IV. E XPERIMENTAL R ESULTS                                Inc., Switzerland) was used to track the motions of both the
                                                                           master device and the robot arm in real time. Passive infrared
A. Experimental Setup for Performance Evaluation                           markers were mounted to the control stick of the master device
  To evaluate position control accuracy during teleoperation               and the test tool attached the end joint of the robot arm.
of serial robot arm by the developed master device, an                     Although the OTS tracked the markers attached to the master
experimental system was configured as shown in Figure 7.                   device’s control stick and the robot arm’s tool, using marker
The experimental setup consisted of the master device, master              pivoting, the system accurately determines the pose of the
controller, control computer for the master device, OTS, data              master device’s top plate center and the robot arm’s TCP. This
logger, and a serial robot arm. The UR 3e serial robot arm                 setup facilitated a direct comparison of the motions of the
(Universal Robot Inc., USA) was used, and the Real-Time                    master device and robot arm.
